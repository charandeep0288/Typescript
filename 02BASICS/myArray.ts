// const superHeros: number[] = [];
const superHeros: string[] = [];
// const heroPower: number[] = []; // no good no bad
const heroPower: Array<number> = []; // no good no bad

type User = {
    name: string;
    isActive: boolean
}

const allUsers: User[] = []

const MLModels: number[][] = [
    [255, 255, 255],
    // "" // array of array are allowed only, string not allowed 
    []
]




superHeros.push("spiderman");
heroPower.push(2);

allUsers.push({name: "", isActive: true});


export {}